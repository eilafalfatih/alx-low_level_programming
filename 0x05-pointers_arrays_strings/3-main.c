#include "main.h"

/**
 * main -check the code
 *
 * Return: always 0.
 */
int main(void)
{
	char *str;

	str = "I do not fear the computer. I fear the lack of them - Isaac asimov";
	_puts(str);
	return (0);
}
